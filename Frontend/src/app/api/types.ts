export enum Status {
    PENDING = "PENDING",
    AVAILABLE = "AVAILABLE",
    REJECTED = "REJECTED",
    APPROVED = "APPROVED",
    RESERVED = "RESERVED",
    SOLD = "SOLD",
    PAYMENT_ACCEPTED = "PAYMENT ACCEPTED"
}

export enum Role {
    ADMIN = "ADMIN",
    USER = "USER"
}