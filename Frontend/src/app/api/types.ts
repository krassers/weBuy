export enum Status {
    PENDING = "PENDING",
    AVAILABLE = "AVAILABLE",
    REJECTED = "REJECTED",
    APPROVED = "APPROVED",
    RESERVED = "RESERVED",
    SOLD = "SOLD"
}

export enum Role {
    ADMIN = "ADMIN",
    USER = "USER"
}